<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DigitalBalance" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:colorAccent">@color/launch_accent</item>
        <item name="android:statusBarColor">@color/launch_background</item>
        <item name="android:navigationBarColor">@color/launch_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.DigitalBalance.Starting" parent="Theme.DigitalBalance">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>
</resources>
